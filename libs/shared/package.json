{
    "name": "@crates/shared",
    "module": "src/main.ts",
    "type": "module",
    "main": "dist/main.js",
    "types": "dist/main.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "format": "bunx biome format src --write .",
        "lint": "bunx biome lint --error-on-warnings src",
        "lint:ts": "tsc --noEmit"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
